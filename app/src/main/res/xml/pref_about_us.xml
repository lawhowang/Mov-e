<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <Preference android:title="App Name"
        android:key="appName"
        android:summary="@string/app_name"/>
    <Preference android:title="Produced By"
        android:key="producedBy"
        android:summary="Group 7"/>
    <Preference android:title="Group Members"
        android:key="groupMembers"
        android:summary="Louis Siu\nTimothy Lam\nTom Law"/>
</PreferenceScreen>
